# OpenAI speech-to-text. Keep the real value in .env.voice (ignored by Git).
VOICE_STT_API_KEY=
VOICE_STT_API_MODEL=gpt-4o-transcribe
# Without a key, the prototype falls back to a small CPU Whisper model.
